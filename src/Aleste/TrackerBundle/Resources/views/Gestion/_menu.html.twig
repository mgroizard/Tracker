	     <!-- Fixed navbar -->
    <div class="navbar navbar-default navbar-fixed-top" role="navigation">    
      <div class="container-fluid">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="sr-only">Cambiar navegación</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="#"><i class="fa fa-briefcase"></i> {{ global.app.organizacion_corto }}</a>
        </div>
        <div class="navbar-collapse collapse">
          <ul class="nav navbar-nav">
            
            {% if is_granted('ROLE_ADMIN') %}
                {% include 'TrackerBundle:Gestion:_submenu_parametros.html.twig' %}
            {% endif %} 
          </ul>
          <ul class="nav navbar-nav navbar-right info-nav">
           {% if is_granted('ROLE_GESTION') %}
              {{ render(controller('TrackerBundle:Gestion:submenuInfo')) }}
            {% endif %} 
        
            {% if is_granted('ROLE_USER') %}
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="fa fa-flag"></i>  {{'_idiomas'|trans}} <b class="caret"></b></a>
              <ul class="dropdown-menu">              
                <li><a href="{{ path('CommonBundle_set_locale',{'idioma':'es'}) }}">{{'_español'|trans}} <img src="{{asset('bundles/common/images/flags/es.png')}}"/></a></li>
                <li><a href="{{ path('CommonBundle_set_locale',{'idioma':'en'}) }}">{{'_ingles'|trans}} <img src="{{asset('bundles/common/images/flags/us.png')}}"/></a></li>
              </ul>
            </li>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="fa fa-user"></i>  {{app.user}} <b class="caret"></b></a>
              <ul class="dropdown-menu">              
                <li><a href="{{ path('usuario_view',{'id':app.user.id}) }}"><i class="fa fa-edit"></i>  Mis datos</a></li>
                <li><a href="{{ path('logout') }}"><i class="fa fa-sign-out"></i> Salir</a></li>              
              </ul>
            </li>
            {% endif %}
          </ul>
        </div><!--/.nav-collapse -->
      </div>
    </div>